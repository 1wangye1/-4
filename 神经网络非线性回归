import tensorflow as tf
import numpy as np
import matplotlib.pyplot as plt

x_data = np.linspace(-4,4,300)[:, np.newaxis]
noise = np.random.normal(0, 1, x_data.shape)
y_data = np.square(x_data) - 25 + noise

plt.plot(x_data, y_data)
